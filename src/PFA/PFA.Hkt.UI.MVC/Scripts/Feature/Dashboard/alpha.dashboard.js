app.controller("HomeCtrl", ['$scope', '$http', '$window', '$q', 'customGridFactory', function ($scope, $http, $window, $q, customGridFactory) {
    //Required angular scope model declaration.
    $scope.helpers = iAlphaUtility.helpers;
    $scope.grid = {};
    $scope.init = function () {
        
        
    };
    $scope.setupControls = function () {        
        
    };
    
}
]);

